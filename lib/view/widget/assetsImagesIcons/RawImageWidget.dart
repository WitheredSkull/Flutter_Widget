import 'package:flutter/material.dart';

class RawImageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
//    return RawImage(image: Image(image: null,),);
    return Text("是我Low了，不会用 不会用");
  }
}
