import 'package:flutter/material.dart';

class FlutterLogoWidget extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    ///这个我就不说了，很纯粹的一个Logo，有需求自己看源码吧
    return FlutterLogo(
      size: 100,
    );
  }

}